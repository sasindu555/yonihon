import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "YoNihon — Authentic Japanese Cultural Experiences",
    template: "%s — YoNihon",
  },
  description:
    "Discover unique Japanese experiences. Book local workshops, cultural activities, and hidden gems curated by our Tokyo team.",
  openGraph: {
    title: "YoNihon — Authentic Japanese Cultural Experiences",
    description:
      "Discover unique Japanese experiences. Book local workshops, cultural activities, and hidden gems curated by our Tokyo team.",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
